# Product-like replay fixture

This fixture contains only a failing starting state, acceptance bodies, and one prior engineering decision. It intentionally does not contain the expected implementation. The replay runner copies it into a disposable Git repository before starting the installed candidate.
