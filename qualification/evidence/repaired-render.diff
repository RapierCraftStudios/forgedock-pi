diff --git a/src/render.mjs b/src/render.mjs
index c0adf72..6a1094b 100644
--- a/src/render.mjs
+++ b/src/render.mjs
@@ -1,5 +1,6 @@
 import { normalizeDisplayName, profileId } from "./profile.mjs";
 
 export function renderProfile(profile) {
-  return `${profileId(profile)}:${normalizeDisplayName(profile.name)} [${profile.team}]`;
+  const teamLabel = profile.team === undefined ? "" : ` [${profile.team}]`;
+  return `${profileId(profile)}:${normalizeDisplayName(profile.name)}${teamLabel}`;
 }
