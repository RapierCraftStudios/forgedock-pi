These prompt templates are intentionally limited to the four familiar ForgeDock entrypoints.
Use the slash aliases (`/work-on`, `/orchestrate`, `/review-pr`, and `/review-pr-staging`)
through the candidate extension. Workflow behavior is in the corresponding candidate skills.
